generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum AccountType {
  useradmin
  userdefault
}

enum AccountStatus {
  ativo
  inativo
}

model User {
  id                     String        @id @default(cuid())
  status                 AccountStatus
  type                   AccountType
  name                   String?
  cpf                    String
  genero                 String
  dateOfBirth            String
  email                  String        @unique
  phone                  String
  password               String?
  resetPasswordToken     String?
  resetPasswordExpires   DateTime?
  emailVerified          DateTime?
  emailVerificationToken String?
  googleId               String?       @unique
  avatar                 String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("users")
}

// model Produto {
//   id            String   @id
//   title         String
//   price         Float
//   old_price     Float
//   assessment    Int
//   description   String
//   products_sold Int
//   endDate       DateTime
//   bigsale       Boolean
//   sku           String   @unique
//   stock         Int
//   category      String
//   brand         String
//   tags          String[]
//   options       Json     // Pode ser usado para armazenar as opções de tamanho e cor
//   images        Image[]  // Relacionamento com a tabela de imagens
// }

// model Image {
//   id       String @id
//   image    String
//   productId String
//   product  Produto @relation(fields: [productId], references: [id])
// }
